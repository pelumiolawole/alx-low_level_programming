#include "main.h"
#include <stdio.h>

/**
 *_strlen - finds and outputs length of string.
 *@s: pointer to string.
 *
 *Return: length of a string.
 */
int strlen(char *str)
{
    int len;
    len = strlen(char *str)
	printf("%d", len)
	return (0);
}
